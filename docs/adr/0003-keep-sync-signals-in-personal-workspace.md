# Keep sync signals in the personal workspace

Sync-specific visibility belongs only in the personal workspace. The engine adds the per-workspace sync label and the external issue link to the personal issue, while external issues receive no sync label, backlink, comment, or other sync-specific marker. This preserves the user's central-hub workflow and keeps participation in the sync engine private from external workspaces.
