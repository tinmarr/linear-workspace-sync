# Exclude archived issues

Archived issues are excluded from synchronization and inbound discovery across all workspaces, even when they remain assigned. Non-archived assigned issues are eligible regardless of status. This matches the workspaces' auto-archive behavior and prevents completed historical work from reappearing after archival.
