# Use explicit creation destinations

Every connected workspace has an explicit configured creation team. The MVP currently uses one team per workspace, but the configuration must not rely on workspace defaults or inferred placement. This keeps issue creation deterministic and leaves room for multiple teams later.
