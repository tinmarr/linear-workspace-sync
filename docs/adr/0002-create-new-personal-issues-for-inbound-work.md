# Create new personal issues for inbound work

When an assigned external issue has no existing sync mapping, the engine creates a new personal issue rather than attempting to match by title or other inferred similarity. Existing issues can be paired only through an explicit manual mapping, keeping identity deterministic and making duplicate creation visible and reversible.
