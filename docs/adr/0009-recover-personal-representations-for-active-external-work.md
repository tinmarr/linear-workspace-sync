# Recover personal representations for active external work

An active external issue assigned to the user must remain represented in the personal workspace. If its personal issue is archived, the engine restores it; if it was deleted and cannot be restored, the engine creates a replacement personal issue and reconnects the one-to-one mapping. The external issue is never changed as part of this recovery.
